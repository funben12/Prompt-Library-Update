# 03_MVP_Scope — CLAUDE.md

## Purpose

Historical record of what counted as MVP for this product, and the V1→V2 lesson learned from it.

## Rules for Claude

- This document is historical, not a live scoping tool for this mature product — new feature scoping should reference the Roadmap section of root `MEMORY.md` instead.
- If Eugene starts a genuinely new feature that needs its own MVP definition (e.g. a major new workspace), that's a candidate for its own mini-scope note here, appended below the historical section — don't overwrite the V1/V2 history.
