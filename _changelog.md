[04-06-2026 22-28] CREATED — 04-06-2026 - churn-analysis-and-improvements.md — v1 saved — 10-item churn analysis + improvement roadmap: in-app executor, analytics, roles onboarding, marketplace, conditional variables, web companion, first-run wizard, free tier restructure, community hub, free version history
[18-05-2026 19-26] SESSION OPEN — Rollback system initialised — _rollbacks folder created — v1 snapshots saved for app.py, app.js, index.html — Feature build session: Meta Prompting + Chain Prompting with phase output and clipboard copy
[18-05-2026 19-32] MODIFIED — static/app.js — v2 saved — Replaced chain runner (step-based) with phase-based runner: PL_copyChainPhase, PL_showOutputCapture, PL_captureAndNext, PL_skipCapture, PL_finishChain, PL_exportFullChain — AI response carry-forward, completion summary, full chain export
[18-05-2026 19-32] MODIFIED — static/index.html — v2 saved — Added CSS: .chain-phase-context, .chain-output-capture, .chain-phase-summary, .chain-output-preview
[18-05-2026 19-32] ACTION — update_hash.py run — app.js hash updated to a3d3708c
[18-05-2026 21-11] MODIFIED — static/app.js — v4 saved — Added updateVariablePreview(), copyFilledBtn wiring, event listeners on #variableFields; Full roles workspace: openRolesWorkspace, loadRoles, renderRolesList, openRoleInEditor, PL_saveRole, PL_newRole, PL_deleteRole, PL_duplicateRole, PL_toggleRoleFav, PL_copyRolePersona; nav wire for roles view
[18-05-2026 21-11] MODIFIED — static/index.html — v4 saved — Live preview HTML (#variablePreview, #variablePreviewText, #copyFilledBtn), Roles nav button, Roles modal HTML, Roles + preview CSS blocks
[18-05-2026 21-11] ACTION — update_hash.py run — app.js hash updated to 4ad1a169
[18-05-2026 21-25] MODIFIED — Build.bat — v2 saved — Added taskkill step before clean (prevents Error 5 on locked EXE); added --noupx flag to pyinstaller command (UPX triggers Defender scans which lock the output EXE mid-build)
[18-05-2026 21-25] MODIFIED — Build.bat — v2 saved — Fix Error 5/Access Denied: (1) taskkill /F /IM PromptLibrary.exe before clean step + 2s wait to release file handles; (2) --noupx flag added to pyinstaller command — UPX triggers Defender AV scan which locks EXE mid-build
[18-05-2026 21-27] SNAPSHOT — Full rollback point saved — app.js v5, index.html v5, app.py v2, Build.bat v3 — State: chain phasing + live variable preview + roles workspace + build fix all complete
[18-05-2026 21-32] SESSION AUDIT — MEMORY.md updated: Build.bat Error 5 fix, Edit tool truncation rule, Current State date → 2026-05-18, chain runner phase functions, roles workspace actual functions — CLAUDE.md updated: Python threshold for large edits, script tag baseline corrected to 2 in three places
[19-05-2026 10-57] ROLLBACK — app.js restored to v3, index.html restored to v3 — Roles workspace + variable preview removed — Chain phasing preserved — Reason: app broken after roles/preview additions

[19-05-2026 21-56] ADD — index.html — v8 rollback saved — Added Roles nav button to sidebar nav; added full Roles workspace HTML (two-pane: list + editor + Build Prompt section with 3 copy formats); added Roles workspace CSS — Roles workspace frontend build
[19-05-2026 21-56] ADD — app.js — v8 rollback saved — Added _rolesState, loadRoles, renderRolesList, openRoleInEditor, buildRolePrompt (structured/xml/prose), updateRolePromptPreview, getRoleFromForm, PL_copyBuiltPrompt, PL_copyRolePersona, PL_saveRole, PL_newRole, PL_deleteRole, PL_duplicateRole, PL_toggleRoleFav, initRolesWorkspace — Full Roles workspace JS
[19-05-2026 21-56] ADD — app.js — v8 rollback saved — Added updateRoleDropdown(); wired role_id into openNewPromptModal, editPrompt, handlePromptSubmit — Role assignment in prompt editor modal
[19-05-2026 21-56] ADD — app.js — v8 rollback saved — Extended _chainRunnerState with roles cache; startChainRunner fetches role data per step; renderChainRunner shows role badge per step; PL_copyChainPhase prepends role persona as system context block — Role integration in chain runner
[19-05-2026 21-56] RUN — update_hash.py — hash a01d647a — Cache-bust hash updated after app.js changes

[19-05-2026 22-13] FIX — app.js — v9 rollback saved — Fixed PL_filterByCategory and PL_filterByTag: no longer call setView() (which wiped filterPill); instead set state.view directly to avoid side-effect clear, then call setFilterPill — categories/tags now filter correctly on click
[19-05-2026 22-13] REMOVE — app.js — v9 rollback saved — Removed unsorted/untagged view branches from setView(), renderPrompts() filter scope, updateCounts(), and init() nav click handler — Unsorted/Untagged views eliminated
[19-05-2026 22-13] REMOVE — index.html — v9 rollback saved — Removed Unsorted and Untagged nav buttons from sidebar. Restored from v8 and reapplied all changes cleanly in single Python pass to fix prior truncation. All Roles workspace HTML, CSS, and role dropdown re-applied.
[19-05-2026 22-13] RUN — update_hash.py — hash 63efa3d5 — Cache-bust hash updated

[19-05-2026 SESSION-2]
---
FIX — app.py — v5 — /api/starter-templates route added and indentation error corrected — starter prompts now load when library is empty
FIX — app.js — v11 — chain runner Phase 1 variable fill: hidden vars get hidden inputs with defaults applied; visible vars show default values; live preview added (updates as user types, shows substituted prompt text)
FIX — app.js + index.html — v11/v10 — Chat Format: added Copy turns button with Plain text / ChatML / OpenAI JSON format selector — PL_copyChatTurns() function
FIX — app.js — v11 — Variable manager: up/down reorder buttons added to var-meta-row; collectVarMeta saves sort_order; renderVariableFields and renderVarMetaList both sort by sort_order; PL_moveVar() added
HASH — app.js — cache-bust hash updated to 21346837

[20-05-2026 SESSION-3]
---
RESTORE — app.js — v12 (corrupted NUL-contaminated state) backed up, v11 (clean) restored — File had 5248 NUL bytes, syntax error
RESTORE — index.html — v11 (corrupted truncated state) backed up, v10 (clean) restored — File was truncated mid-tag, missing </body></html>
FIX — app.js — v13 rollback saved — updateRoleDropdown: replaced stacking addEventListener (once:false) with sel.onchange assignment — prevents duplicate listener accumulation on repeated modal opens; also syncs #promptRoleId immediately on load
ADD — app.js — v13 rollback saved — PL_copyChatTurns() implemented: reads turns from #promptChatTurns, formats as Plain text / ChatML / OpenAI JSON based on #chatCopyFormat selector, copies to clipboard
ADD — index.html — v12 rollback saved — Copy turns UI added to #pane-chat: format selector (plain/chatml/openai) + Copy turns button wired to PL_copyChatTurns()
VERIFIED — tag/category filters — PL_filterByCategory and PL_filterByTag both use state.view direct assignment (not setView()) — v9 fix intact
HASH — app.js — cache-bust hash updated to dfd3e52b

[20-05-2026 SESSION-3 CONTINUED]
---
DEBUG — Full code review and debug audit performed: 4 bugs identified in roles workspace
FIX — app.js — v14 rollback saved — Bug 1: removed duplicate click listener on #rolesNavBtn from initRolesWorkspace() — init() already handles all nav-item[data-view] clicks; double listener caused openRolesWorkspace() to fire twice per click
FIX — app.js — v14 rollback saved — Bug 2: simplified triple display assignment in openRoleInEditor (form.style.display='flex' x3 with removeProperty in middle) to single assignment
FIX — app.js — v14 rollback saved — Bug 4 (showstopper): exposed openRoleInEditor on window — onclick attributes in renderRolesList() run in global scope; without window exposure clicking any existing role silently did nothing
FIX — app.js — v14 rollback saved — Bug 5: fixed broken icon picker guard (charAt(0) === '' is always false) — replaced with val && val.trim() check
RESTORE — index.html — restored from rollback v12 (complete, 2 script tags) after mount sync truncation detected — v13 rollback saved

[04-06-2026 14-30] CREATED — PROMPT_LIBRARY_PRO_SPEC.md — v1 — Comprehensive PRD (3000+ words covering: problem statement, goals/non-goals, personas, 10 core features, technical architecture, DB schema, API endpoints, frontend patterns, license model, free tier limits, roadmap (V1.1-V3.0), success metrics, open questions, FAQ) — Full ship-ready + vision spec — Product specification complete — Rollback v1 saved
HASH — app.js — cache-bust hash updated to 82d25445

[20-05-2026 SESSION-3 KB BUILD]
---
ADD — app.py — v6 rollback saved — knowledge_base TEXT column migration added to init_db for roles table; serialize_role parses JSON array; _role_payload sanitises and serialises entries; create_role, update_role, duplicate_role all updated to include knowledge_base
ADD — app.js — v15 rollback saved — renderKbList(), PL_addKbEntry(), PL_removeKbEntry(), PL_toggleKbInclude(), _getKbFromDOM() added; openRoleInEditor renders KB on load; PL_newRole clears KB; getRoleFromForm collects KB entries; buildRolePrompt appends include=true entries in structured/xml/prose formats
ADD — index.html — v15 rollback saved — KB section HTML inserted between roleExampleInput and rolePromptBuilder: header with info tooltip, Add entry button, #kbEntriesList container; KB CSS block added (kb-section, kb-entry-card, kb-include-label, kb-tooltip)
HASH — app.js — cache-bust hash updated to f98a0f86
[20-05-2026 Session 4] EDIT — static/app.js — v16 saved — DOMContentLoaded: parallelised loadStoredLicence() + loadAll() with Promise.all() — eliminates serial licence-check stall before prompts render
[20-05-2026 Session 4] EDIT — static/index.html — v16 saved — Added skeleton shimmer CSS + 5 skeleton cards in #promptsContainer — UI shows placeholder cards immediately on first paint instead of blank pane
[20-05-2026 Session 4] RUN — update_hash.py — hash updated to c481e7b5

[20-05-2026 16-17] ADDED — static/index.html — v18 saved — Agent Builder nav button (premium-locked, below Roles), Agent Builder CSS (~260 lines, inherits app vars), Agent Builder workspace HTML (3-col layout: side-nav, 8-section form, preview panel) — New premium workspace feature
[20-05-2026 16-17] ADDED — static/app.js — v18 saved — Agent Builder IIFE: AB_TEMPLATES (16 templates), AB_TONES/STYLES/AUDIENCES data, openAgentBuilderWorkspace, closeAgentBuilderWorkspace, initAgentBuilder, live preview builder, save/load/delete via /api/agent-prompts, copy, export, reset, collapse cards, nav highlight, wired into setView handler — Agent Builder JS engine
[20-05-2026 16-17] ADDED — app.py — v8 saved — agent_prompts DB table added to init_db; GET/POST/PUT/DELETE routes at /api/agent-prompts — Backend persistence for Agent Builder saved prompts
[20-05-2026 11-00] EDIT — index.html — v20 saved — Agent Builder spacing overhaul: increased form-area padding (sp-4→sp-5), card-body padding (sp-3→sp-4), card/preview headers (10px→13px), input padding (6px→8px), label margin (3px→5px), inter-card gap (sp-3→sp-4), preview panel width (340→380px), side nav width (210→220px), nav item padding (6px→8px). Reason: layout too cramped, insufficient breathing room across all sections.
[20-05-2026 21-45] RESTORED — static/index.html — v21 saved (broken file preserved) — Restored from v20 — Live index.html was truncated mid-file (0 script tags, no </body></html>, divs 273/267, content cut inside Knowledge Base section), causing Agent Builder + Library to render stacked on top of each other — v20 was last known-good (2 scripts, complete, 286/286 divs) and contained the more complete Agent Builder markup (corruption had eaten 4 ab-card blocks)
[20-05-2026 21-45] ACTION — update_hash.py run — app.js hash updated to b174116e

[20-05-2026 21-52] RESTORE — static/app.js + static/index.html + app.py — current state backed up as app.js v19 / index.html v22 / app.py v9 — Restored to last clean pre-agent-builder build: app.js v17, index.html v17, app.py v7. Reason: user requested rollback to full working version without the agent builder. Agent builder was introduced 16-17 across all three files; v17/v17/v7 (16-06/16-07) is the last coherent agent-free snapshot. Knowledge Base feature predates the cut and is retained. Live index.html was also truncated (0 scripts) before restore — now 2 scripts, complete.
[20-05-2026 21-52] ACTION — update_hash.py run — app.js cache-bust hash set to c481e7b5
[23-05-2026 12-23] MODIFIED — app.py — v1 rollback saved — Added skills JSON column migration to roles table; updated serialize_role, _role_payload, create_role, update_role, duplicate_role to include skills; added /api/save-file route with tkinter folder picker — Feature: Skills section in Roles, folder-picker export
[23-05-2026 12-23] MODIFIED — static/index.html — v1 rollback saved — Added Skills card section and CSS to roles editor; added Template tab and panel to import modal; added import-template CSS — Feature: Skills section in Roles, Template tab in import modal
[23-05-2026 12-23] MODIFIED — static/app.js — v1 rollback saved — Added renderSkillList, PL_addSkillEntry, PL_removeSkillEntry, _getSkillsFromDOM; updated buildRolePrompt (all 3 formats), getRoleFromForm, openRoleInEditor, PL_newRole for skills; added import template functions; updated exportJson/exportFormat to use /api/save-file — Feature: Skills, Template tab, folder-picker export
[23-05-2026 12-23] HASH UPDATE — static/index.html — cache hash updated to 9d72a0e0 — app.js modified
[23-05-2026 Session 4] MODIFY — static/app.js — v3 saved — Added: renderExampleList, _getExamplesFromDOM, PL_addExamplePhrase, PL_removeExamplePhrase, initConfigPanel, loadConfigSettings, PL_generateRoleWithAI. Updated openRoleInEditor, buildRolePrompt (all 3 formats), getRoleFromForm, PL_newRole, initRolesWorkspace to use multi-phrase system. Fixes example clear-on-save bug.
[23-05-2026 Session 4] MODIFY — app.py — v2 saved — Added example_phrases JSON column migration, updated serialize_role, _role_payload, create_role, update_role, duplicate_role, starter template seeding. Added /api/settings/ai-config GET/POST routes.
[23-05-2026 Session 4] HASH — static/index.html — 454e6835 — Cache-bust hash updated after app.js changes.

[23-05-2026 19-48] DEBUG OVERHAUL — full-app audit + fixes — versions: app.py v12, app.js v23, index.html v25, PromptLibrary.iss v3
[23-05-2026 19-48] FIX — app.py — v12 — C1: create_role INSERT had 9 placeholders for 10 columns; added 10th. New roles can now be created (was a hard 500). — root cause of dead Roles workspace
[23-05-2026 19-48] FIX — app.py — v12 — H1: added missing /api/export/bulk route (zips prompts.json + prompts.md). Bulk-export Pro button was a 404.
[23-05-2026 19-48] FIX — app.py — v12 — H2: import_json INSERT now includes role_id. Re-imported prompts no longer lose their role.
[23-05-2026 19-48] FIX — static/app.js — v23 — C2: added shared fetchRoleObj()/prependRole(); wired into useFromCard + handleCopyWithVariables so a prompt's role now copies WITH it. Chain runner refactored to use the same helper (removed duplicated header logic).
[23-05-2026 19-48] FIX — static/app.js — v23 — C3: renderDetailPanel now shows assigned role via renderDetailRole() + #detailRoleChip.
[23-05-2026 19-48] FIX — static/app.js — v23 — M1: loadRoles fetches /roles/<id>/prompt-count; roles list shows usage count badge.
[23-05-2026 19-48] FIX — static/index.html — v25 — C3: added #detailRoleChip element + .detail-role-chip/.role-item-count CSS. div balance 233->234 (expected).
[23-05-2026 19-48] FIX — PromptLibrary.iss — v3 — M2: AppDataFolder "Prompt Library" -> "PromptLibrary" to match app.py get_data_dir(). Fixes pre-created folder, data shortcut, uninstall message.
[23-05-2026 19-48] BUILD — static/index.html — cache-bust hash updated to 2ba64588 via update_hash.py.
[23-05-2026 22-13] FIX — static/app.js — v24 — L1: replaced fragile 'data.roles || data' with Array.isArray() guard in updateRoleDropdown + loadRoles. Robust if /api/roles response shape ever changes.
[23-05-2026 22-13] DECISION — L3 (legacy example_phrase column) left in place — still load-bearing for serialize_role, _role_payload, duplicate_role, .plp import, and frontend XML output. Removal = 6-site schema change, out of scope.
[23-05-2026 22-13] BUILD — static/index.html — cache-bust hash updated to 38bad9b0 via update_hash.py.

[23-05-2026 22-33] RECOVERY — static/index.html — restored from v25 after Windows-mount truncation (live file cut to 117727 bytes mid-tag, 0 scripts). Re-applied C3 role-chip. Truncated copy kept as _rollbacks/index.html/index.html - TRUNCATED-22h13-forensic.
[23-05-2026 22-33] FEATURE — static/index.html — v26 — Rebuilt Unlock Pro modal as two-column Free | Pro pricing split (Free muted card, Pro accent card with 'Most popular' ribbon). Accurate tier split from premium-locked audit. Kept licence/activate IDs.
[23-05-2026 22-33] FEATURE — static/index.html — v26 — Buy CTA now points to Payhip (https://payhip.com/MXPhillips); Ko-fi link removed. 'Get your licence key on Payhip' + 'Buy on Payhip'.
[23-05-2026 22-33] FEATURE — static/index.html + static/app.js — v26/v25 — Gated Roles 'Generate with AI' to Pro: premium-locked + data-premium on #rolesGenerateBtn; state.isPremium guard in PL_generateRoleWithAI. Roles workspace stays free; only AI generation is Pro.
[23-05-2026 22-33] BUILD — static/index.html — cache-bust hash updated to e5959e3c via update_hash.py.

[23-05-2026 22-42] FEATURE — app.py — v13 — Added 15 sales licence keys as SHA-256 hashes only (_SALES_KEY_HASHES, unioned into PREMIUM_KEYS). Plaintext NEVER in source — lives in keys_PRIVATE.txt for Payhip upload. 12 existing keys kept. Verified: all 15 validate, 0 plaintext leaked, compiles.
[23-05-2026 22-42] CREATE — keys_PRIVATE.txt — 15 plaintext keys (16-char, unambiguous alphabet). PRIVATE: upload to Payhip License Keys, keep a backup, do NOT ship.

[23-05-2026 22-45] SECURITY — app.py — v14 — Removed 6 weak/guessable keys (Eugene, 1234, PROMPTLIB-PRO-2026, WARRIOR-007, X9F7-8K2M, NorthCarolina357). Kept 6 structured issued keys + 15 sales keys = 21 valid. Verified: weak keys now rejected, kept+sales keys still validate.
[23-05-2026 22-45] SECURITY — static/app.js — v26 — showPremiumModal no longer pre-fills the licence box (was state.licenceKey). Box always starts empty so pressing Enter/Activate with no input cannot unlock. Empty input confirmed rejected.
[23-05-2026 22-45] BUILD — static/index.html — cache-bust hash updated to  via update_hash.py.

[27-05-2026 02-52] ADD — app.py — v15 — Added /api/playground/* routes (sessions CRUD, panels save/restore, seed-from-prompt). SQLite tables: playground_sessions, playground_panels. — Prompt Playground feature
[27-05-2026 02-52] ADD — static/app.js — v27 — Added full Prompt Playground JS module: open/close, session CRUD, 1/A-B/3-way panel modes, score stars, char count, "Save to library" shortcut, Ctrl+Shift+P hotkey, auto-save on blur, initPlaygroundWorkspace() bootstrap call. — Prompt Playground feature
[27-05-2026 02-52] ADD — static/index.html — v29 — Added Playground nav item in sidebar, full #playgroundWorkspace HTML block, all CSS (pg-* classes). — Prompt Playground feature
[27-05-2026 02-52] UPDATE — static/index.html — v29 — Cache-bust hash updated to 99cf0b2e after app.js change. — update_hash.py
[27-05-2026 00-00] MODIFY — static/app.js — v28 — Fixed _pgSaveToLibrary to call api('/prompts') directly instead of opening modal; removed color dot span from renderRolesList — three fixes session
[27-05-2026 00-00] MODIFY — static/index.html — v31 — Replaced Roles workspace CSS + HTML (removed color swatch column, semantic tags, 2-col meta row, hidden color picker); Playground workspace CSS + HTML added from prior session
[27-05-2026 00-00] MODIFY — static/index.html — hash updated to 2f49ee71 after app.js changes

[27-05-2026 19-13] RESTORE — index.html — v33 saved — Restored from v31 (clean base); CSS block already applied last session
[27-05-2026 19-13] REPLACE — index.html — v34 saved — Replaced old ROLES WORKSPACE HTML block with full AGENT BUILDER WORKSPACE HTML (section tabs: Core/Behaviour/Context/Workflow/Knowledge/Preview; sidebar stats panel; all ab-* element IDs)
[27-05-2026 19-13] UPDATE — index.html — no version — Nav button: person_play → smart_toy, label Roles → Agents, manage-roles icon updated to match
[27-05-2026 19-13] UPDATE — index.html — no version — Cache-bust hash updated to 2adbba4b after app.js changes
[27-05-2026 19-13] VERIFY — all files — n/a — node --check app.js: OK | py_compile app.py: OK | scripts:2 complete:True NUL:0 | div balance:0 | hash:2adbba4b

[27-05-2026 SESSION-5] RESTORE — static/index.html — v36 saved — Discovered that the full Roles workspace layout CSS (#rolesWorkspace, .roles-header, .roles-body, .role-list-item, .roles-editor-pane, .roles-editor-scroll, .roles-editor-toolbar, .role-prompt-builder, .role-icon-picker, .role-meta-row) was completely absent from the live file — only the thin AGENT WORKSPACE block (section dividers + chips + stats) was present, leaving the workspace visually broken
[27-05-2026 SESSION-5] FIX — static/index.html — v36 → v37 (live) — Replaced thin AGENT WORKSPACE CSS block with full merged CSS: all v31 layout styles restored + new section-divider, chip-group, two-col, sidebar-stats styles retained. All section CSS (KB, Skills, Examples, Build Prompt) present and correct. No custom/external CSS — 100% app vars.
[27-05-2026 SESSION-5] VERIFY — static/index.html — scripts:2 complete:True NUL:0 div-balance:0 — node --check JS: OK — py_compile: OK — all 13 CSS class checks: OK
[27-05-2026 SESSION-5] HASH — static/index.html — hash updated to 82db9b8f via update_hash.py

[28-05-2026 SESSION-5] RESTORE — static/index.html — v37 saved — Rebuilt Playground workspace: CSS block (8875 chars, all app vars) re-injected from v29-truncated-backup; #playgroundWorkspace HTML reconstructed from app.js structure (pgPanelsGrid, pgSessionsList, pgCanvasToolbar, pgSessionTitleInput, pgSessionNoteInput, pgNewSessionBtn, pgSaveAllBtn, pgCloseBtn, pgEmptyState); playground nav button (playgroundNavBtn, data-view="playground") added to sidebar after rolesNavBtn. Feature fully restored — was silently broken since 21:52 session recovery on 27 May.
[28-05-2026 SESSION-5] VERIFY — static/index.html — scripts:2 complete:True NUL:0 div-balance:0 — JS OK — PY OK — all 13 playground+roles checks: OK
[28-05-2026 SESSION-5] HASH — static/index.html — hash updated to 82db9b8f

[28-05-2026 SESSION-5 DEBUG] FIX — app.py — v16 saved — Added 6 missing role DB column migrations: audience, output_format, constraints, domain, tasks, response_style. All 6 added to _role_payload return dict and serialize_role setdefaults. Fields now fully persisted: agent editor saves/loads Behaviour, Context, and Workflow sections correctly.
[28-05-2026 SESSION-5 DEBUG] RESTORE+REINJECT — static/index.html — v38 saved (clean final state) — Mount truncated file mid-session (scripts dropped to 0). Restored from v37, re-applied playground CSS (8875 chars from v29-truncated-backup) + workspace HTML + nav button via atomic fsync write. All 11 checks pass: scripts:2, complete:True, NUL:0, div-balanced, pg CSS, pg workspace div, pg nav btn, pgSessionsList, pgPanelsGrid, pgCanvasToolbar, roles CSS, roles workspace. Hash: 82db9b8f.
[28-05-2026 00-00] MODIFIED — index.html — v40 saved — Moved API config panel: replaced floating fixed-position gear button (#configToggleBtn) with sidebar-footer-btn matching Import/Export/Dark mode style; #configPanel CSS updated to slide up from sidebar bottom (translateY animation) instead of left-side flyout. — Eugene requested config panel stop covering the library.
[28-05-2026 00-30] MODIFIED — app.js — v31 saved — Fixed ReferenceError in buildRolePrompt: added missing const declarations for complexity, initInstr, memCtx, procType. In strict mode these were crashing the live preview on every keystroke. Also added configCloseBtn handler and active class toggle to configToggleBtn. — Preview was silently dying; close button had no wiring.
[28-05-2026 00-30] MODIFIED — index.html — v42 saved — Added full CSS for config panel internals: .config-panel-header, .config-panel-body, .config-section-label, .config-provider-tabs, .config-provider-tab, .config-api-row. Panel now matches app theme. — Panel was completely unstyled.
[28-05-2026 01-00] MODIFIED — index.html — v44 saved — Replaced rolePromptStarter select: 5 options → 168 deduplicated options. Full comprehensive prompt starter library covering all major role/persona framings. — User provided new complete option list.
[28-05-2026 01-15] MODIFIED — icon.ico + static/icon.ico — replaced — Converted app-icon.png (500×500 RGBA) to multi-size ICO: 16, 24, 32, 48, 64, 128, 256px. Previous icons backed up as .ico.bak. — User provided new app icon image.
[28-05-2026 02-00] MODIFIED — app.js — v33 saved — prependRole() now calls buildRolePrompt(role,'structured') — full role framework (identity→behaviour→context→workflow→KB→skills) prepended on copy, not just persona. Added form-input class to example-phrase-input template so card inputs match KB/skills styling. — User requested full role attachment and uniform input styling.
[28-05-2026 02-00] MODIFIED — index.html — v46 saved — .example-phrase-input CSS trimmed: removed redundant font-size (form-input base class now handles it), kept flex:1. — Styling uniformity pass.

[28-05-2026 14-32] ADDED — app.py — v10 rollback — SHORTCUTS_DB constant, get_shortcuts_db(), shortcuts table in init_db(), full /api/shortcuts CRUD + /api/shortcuts/suggest routes
[28-05-2026 14-32] ADDED — static/index.html — v47 rollback — #shortcutsWorkspace HTML panel (two-pane: list + editor), sidebar nav button (premium-locked), shortcuts CSS block, shortcut-suggest-strip
[28-05-2026 14-32] ADDED — static/app.js — v34 rollback — initShortcutsWorkspace(), openShortcutsWorkspace(), full CRUD JS, Tab-trigger expansion in prompt editor, suggest strip loader; new hash 818a46c7

[28-05-2026 15-12] FIX — static/index.html — v47 restored (Windows mount truncated v48 to missing </body></html>) — reinjected shortcuts HTML atomically — v48 re-saved clean
[28-05-2026 15-12] UPDATED — static/index.html — v49 rollback — neumorphic CSS utilities added (nm-raised, nm-inset, nm-badge, premium-feature-card, premium-features-grid, workspace header depth)
[28-05-2026 15-12] UPDATED — static/index.html — v50 rollback — price-card nm shadows, pro card inset glow, Pro modal feature list updated (Text Expansion + Chat Format + Theme toggle added), nm-badge on shortcuts Pro badge
[28-05-2026 15-12] UPDATED — MEMORY.md — roadmap rewritten: single clean section, full Free vs Pro feature inventory table, planned items tagged Free/Pro

[28-05-2026 21-52] PERF — static/index.html — v51→v52 — extracted 95,574 bytes of inline CSS to static/app.css; added defer to app.js script tag; HTML shrunk from 174KB to 78KB
[28-05-2026 21-52] PERF — static/app.css — new file (96KB) — all styles extracted from index.html; served via /static/ route with hash 332e4a69
[28-05-2026 21-52] PERF — app.py — v11 rollback — removed shortcuts DB init from init_db(); added _ensure_shortcuts_table() lazy helper; called at start of list/create/suggest routes only
[28-05-2026 21-52] UPDATED — update_hash.py — now hashes both app.js and app.css, updates both version params in index.html

[29-05-2026 13-03] REVERT — static/app.js — restored v34 (pre-shortcuts); v35 saved as clean post-revert state
[29-05-2026 13-03] REVERT — app.py — restored v10 (pre-shortcuts routes); v12 saved as clean post-revert state
[29-05-2026 13-03] REVERT — static/index.html — removed shortcutsNavBtn, shortcutsWorkspace HTML, Text Expansion from premium modal; v53 saved
[29-05-2026 13-03] REVERT — static/app.css — removed shortcuts CSS block (4,452 bytes); removed .shortcuts-header from nm depth rule; CSS hash now 2d9b2e6e
[29-05-2026 13-03] NOTE — Text Expansion (shortcuts) fully removed from codebase. app.js hash reverted to 09bb1242. Performance fixes (CSS external, defer) and neumorphic pass retained.

[29-05-2026 13-09] REWRITE — static/app.js — v37 saved — chain runner redesigned: varMaps stored per phase in state, ALL phases show variable fields, variables pre-filled on re-render, per-phase inline output capture, PL_advanceChain/PL_goToPhase added, PL_exportFullChain now substitutes variables and chains captured outputs; hash c9f88ab3

[29-05-2026 13-13] REVIEW — deploy checklist, code review, design check all passed
[29-05-2026 13-13] FIX — static/app.css — done-phase opacity 0.5 → 0.72 (variables unreadable at 0.5); removed dead .chain-output-capture block; CSS hash now ddedcf03
[29-05-2026 13-13] CONFIRM — no shortcuts leakage, no XSS risk in chain runner (all user content via escapeHtml), no orphan HTML references to old chain functions

[29-05-2026 13-21] UPDATED — PromptLibrary.iss — rollback saved; AppName corrected to "Prompt Library Pro"; real GUID A300D0C3...; added ChangesAssociations=yes; custom Welcome/Finish wizard text; DeleteDataOnUninstall checkbox in uninstall form; FriendlyTypeName for .plp; UninstallDelete removes log files; Pascal code cleaned and structured
[29-05-2026 18-45] MODIFIED — static/index.html — v54 saved — Phase 2: replaced prompt modal with enhanced version (system/conversation tabs, category chip grid, private toggle, advanced collapsible section) — PromptBase-style submission UI matched to app design system
[29-05-2026 18-45] MODIFIED — static/app.js — v1 saved — Phase 2: patched categoryTagInput references to chip system; added initCategoryChips(), initPromptBlockTabs(), initConversationButtons(), getChipCategories(), setChipCategories(), resetCategoryChips() — wires new modal UI
[29-05-2026 18-45] MODIFIED — static/app.css — v1 saved — Phase 2: added prompt-block, category chip grid, private toggle, advanced section, form-hint, btn-sm styles inside @layer components
[29-05-2026 18-51] MODIFIED — static/app.js — v2/v3 saved — Phase 2 structural fix: moved all Phase 2 code inside main IIFE (was dangling outside), stripped stray DOMContentLoaded wrapper, patched BOOTSTRAP to call initCategoryChips/initPromptBlockTabs/initConversationButtons; patched switchEditorTab to remap 'content'→'variables', openNewPromptModal to call switchPromptBlockTab('system'), editPrompt to auto-switch conversation tab when turns exist
[29-05-2026 19-17] MODIFIED — static/index.html — v57 saved — Removed private toggle; added Surprise Me button to header toolbar
[29-05-2026 19-17] MODIFIED — static/app.js — v4 saved — Fixed copy to include chat turns; added handleSurpriseMe() with flash animation; wired surpriseMeBtn; fixed broken template literals
[29-05-2026 19-17] MODIFIED — static/app.css — v4 saved — Moved Phase 2 styles outside @layer for WebView2 reliability; added surprise-flash keyframe animation
[29-05-2026 19-22] MODIFIED — app.py — v1 saved — get_data_dir() now always points to Documents/PromptLibrary so dev (start.bat) and packaged EXE share the same database and the 210 existing prompts are visible in both modes
[29-05-2026 20-00] MODIFIED — static/index.html — v58 — Added Prompt Forge + Prompt Lab nav items and workspace HTML
[29-05-2026 20-00] MODIFIED — static/app.js — v5 — Forge/Lab workspace JS; sidebar filter _escapeToLibrary fix; refreshModalCategories; BOOTSTRAP wired; template literal syntax fixed
[29-05-2026 20-00] MODIFIED — static/app.css — v5 — Forge and Lab workspace styles; winner bar; responsive breakpoints
[29-05-2026 20-09] MODIFIED — static/app.js — v6 — Added callAI() helper; Lab Run Both + AI Score; Prompt Chain workspace; Metaprompting workspace; nav wired for chain+meta; chain/meta workspace inits in BOOTSTRAP
[29-05-2026 20-09] MODIFIED — static/index.html — v59 — Added Chain + Meta workspace HTML + nav items; Lab run/AI score buttons + output areas
[29-05-2026 20-09] MODIFIED — static/app.css — v6 — Lab output styles; Chain step/arrow/final styles; Meta split-pane styles; assessment block
[29-05-2026 20-24] MODIFIED — static/app.js — v7 — Chain: removed Run/API execution, added library picker per step (loads prompt content + auto-names step), fixed step label layout
[29-05-2026 20-24] MODIFIED — static/index.html — v60 — Chain: removed Run Chain button and final output section
[29-05-2026 20-24] MODIFIED — static/app.css — v7 — Chain step CSS rewrite: fixed layout, added chain-step-hint, chain-step-picker, chain-step-from-library
[29-05-2026 20-35] MODIFIED — static/app.js — v8 — Filter items use data-attributes instead of fragile onclick/&#39; pattern; _escapeToLibrary closes chain+meta; highlight uses data-filter-cat/tag; collapsible workspace group JS
[29-05-2026 20-35] MODIFIED — static/index.html — Workspaces grouped into collapsible nav section (Forge/Lab/Chain/Meta)
[29-05-2026 20-35] MODIFIED — static/app.css — nav-group collapsible styles
[29-05-2026 21-02] MODIFIED — static/app.js — v9 — Chain save now includes INITIAL INPUT block; empty seed renders as [YOUR INPUT HERE] placeholder; {{input}} in steps replaced with placeholder if seed empty; saves then opens detail panel
[30-05-2026 13-15] MODIFIED — static/app.js — v11 — Chain format matches template ({{INPUT}}, pass-the-output notes, last-step end note); auto-tag function added (AI picks from existing tags/cats/folders only); autoTagBtn wired
[30-05-2026 13-15] MODIFIED — static/index.html — v61 — Auto-tag button added to Tags label in new prompt modal
[30-05-2026 13-15] MODIFIED — static/index.html — v61 — Folders/Categories/Tags sidebar sections collapsed by default (aria-expanded=false)
[30-05-2026 13-15] MODIFIED — static/app.js — v11 — Sidebar toggle reads initial aria-expanded on load to set collapsed state; auto-tag regex fixed
[30-05-2026 13-19] MODIFIED — static/app.js — v12 — Smart tag (no API, keyword scoring); Prompt Score live engine (5 dimensions, heuristic, ring UI); smartTagBtn + live score wired
[30-05-2026 13-19] MODIFIED — static/index.html — v62 — Prompt Score tab in Advanced section; Smart tag + AI tag buttons on Tags label
[30-05-2026 13-19] MODIFIED — static/app.css — v9 — Prompt Score pane styles (ring, dimensions, bars, tips)
[30-05-2026 13:25] RESTORE — index.html — v61 restored — file truncated at v62 (94KB, 0 script tags, no </body></html>); rolled back to v61 (93706 bytes, 2 script tags, complete)
[30-05-2026 13:42] MODIFIED — static/index.html — v64 — Smart Tag + AI Tag buttons on Tags label; Prompt Score tab + pane in Advanced; sidebar Folders/Categories/Tags start collapsed; autoTagBtn ID fix
[30-05-2026 14:10] MODIFIED — static/index.html — v66 — Playground/Forge/Lab/Metaprompting/SmartTag/AITag/PromptScore locked as Pro; Agents trial marker; premium modal updated with limits + locked items
[30-05-2026 14:10] MODIFIED — static/app.js — v14 — FREE_LIMITS (25 prompts/3 folders/5 tags/3 cats); gates in handlePromptSubmit, handleFolderSubmit, createFolderInline; Agents 3-day trial on first save
[30-05-2026 14:10] MODIFIED — app.py — v3 — 100 new sales key hashes appended to _SALES_KEY_HASHES (batch 30-05-2026)
[30-05-2026 14:10] MODIFIED — static/app.css — feat-locked CSS for premium modal free column
[30-05-2026 14:55] MODIFIED — static/index.html — v68 — Token Counter tab (Pro-locked) + pane with summary strip and model list injected by JS
[30-05-2026 14:55] MODIFIED — static/app.js — v16 — updateTokenCounter() + _tokenRow() + live wiring on input/editPrompt/openNewPromptModal
[30-05-2026 14:55] MODIFIED — static/app.css — v11 — Token Counter pane styles: summary strip, model grid, fill bars (safe/warn/danger), status badges, mobile collapse, prefers-reduced-motion
[30-05-2026 session] ADD — index.html — v90 saved — Added 3 premium workspace nav buttons (Context Bank, Prompt Scorecard, Collection Builder) to workspacesItems nav group
[30-05-2026 session] ADD — index.html — v90 saved — Added 3 workspace HTML sections: #contextBankWorkspace, #scorecardWorkspace, #collectionWorkspace — full UI with headers, two-panel bodies, search, editor forms
[30-05-2026 session] ADD — app.css — v3 saved — Added display/layout CSS for 3 new workspaces (.ctx-body, .sc-body, .col-body plus all sub-components)
[30-05-2026 session] ADD — app.js — v40 saved — Added initContextBankWorkspace(), initScorecardWorkspace(), initCollectionWorkspace() IIFEs with full open/close/CRUD logic. Updated _escapeToLibrary() array and init() nav handler. Fixed literal newline in buildReport join(). Hash updated: 82e04b98
[31-05-2026 17-21] ADDED — static/app.css — v4 — Appended: @keyframes modal-in/out, card-pop-in, save-pulse, btn-success-flash, ripple-expand, block-insert, ctx-item-in; .modal-main-wrap layout, .modal-side-panel, context bank panel styles, prompt components panel styles, panel-toggle-btn — Impeccable Design animation layer
[31-05-2026 17-21] ADDED — static/app.css — v4 — Appended: modal close (leaving) animation, #promptModal header layout fix for panel toggles
[31-05-2026 17-21] MODIFIED — static/index.html — v91 — Wrapped #promptModal's .modal-box in .modal-main-wrap; added panel toggle buttons (Context Bank, Components) to modal header; added #promptCtxPanel and #promptComponentsPanel side panel HTML with full content
[31-05-2026 17-21] MODIFIED — static/app.js — v41 — Patched closePromptModal for smooth close animation; save-success pulse on submit btn; card stagger entrance animation in renderPrompts; appended initRipple IIFE; appended initModalSidePanels IIFE (side panel open/close, context bank panel with localStorage read/write, prompt components panel with 10 blocks + 6 frameworks, drag-and-drop to textarea)
[31-05-2026 17-29] MODIFIED — static/index.html — v92 — Replaced <select id="promptFolder"> with custom folder picker: text input + hidden input + dropdown div (folderPickerWrap, folderPickerInput, promptFolder, folderPickerDropdown)
[31-05-2026 17-29] ADDED — static/app.css — v5 — Folder picker dropdown styles: .folder-picker-wrap, .folder-picker-dropdown, .fp-option, .fp-kbd-active, .fp-match highlight, .fp-empty
[31-05-2026 17-29] MODIFIED — static/app.js — v42 — Rewrote updateFolderDropdown() for picker; patched openNewPromptInFolder, editPrompt, createFolderInline to use _setFolderPickerValue; appended initFolderPicker IIFE with keyboard nav, match highlighting, open/close, refresh API
[31-05-2026 session] ADD — app.css — v6 saved — Full Impeccable Design motion system: 15 keyframes (ws-item-in, pl-chip-pop, pl-row-saved, pl-detail-in, content-inserted, pl-nav-pip, canvas-block-in, comp-palette-in, pl-ws-open + existing). Workspace body stagger entrance, nav active pip, button press micro-feedback, chip select pop, drag-over textarea highlight, detail panel refresh. Full Prompt Components workspace CSS (pcw-body, palette, canvas, blocks, footer).
[31-05-2026 session] ADD — index.html — v93 saved — Prompt Components workspace HTML (#componentsWorkspace) with palette grid, drop zone canvas, footer with title input + save. Nav button (extension icon, premium-locked). 
[31-05-2026 session] ADD — app.js — v43 saved — initComponentsWorkspace() IIFE: full drag-and-drop canvas (12 blocks, 6 frameworks), palette click-to-add, canvas block edit/remove/reorder, save-to-library via /api/prompts. initAnimationTriggers() IIFE: workspace entrance patches, row-saved flash, detail-refresh, chip-pop MutationObserver. Fixed literal newlines in JS strings. Hash: 896677b6
[31-05-2026 debug] FIX — app.js — v44 saved — detectVariables(): removed .sort() call, replaced Set with Map to preserve first-occurrence order. Variables now display in the order they appear in the prompt text.
[31-05-2026 debug] FIX — app.py — v19 saved — detect_variables(): removed sorted(), replaced set with ordered dict. API now returns variables in text-order not alphabetical.
[31-05-2026 debug] FIX — app.js — v44 saved — saveToLibrary() in componentsWorkspace: replaced raw fetch() with api() helper, fixed tags/categories to send empty strings not arrays, replaced non-existent loadPrompts() with loadAll(). Save to library now works correctly.
[31-05-2026 debug] FIX — app.js — v44 saved — initAnimationTriggers(): removed window.openDetail patch (was wrapping the function, risking interference with prompt editing). Replaced with safe click event listener on .prompt-card for detail-refresh animation. Hash: 0b64ae46
[01-06-2026 09:54] FIX — static/app.js — v45 — Relocated outer IIFE close from line 6815 to end of file. Three appended IIFEs (initRipple, initModalSidePanels, initFolderPicker) were outside outer scope and couldn't see $, state, toast. Caused ReferenceError in _refreshFolderPicker which propagated into openNewPromptModal at line 1666 (updateFolderDropdown), preventing modal from opening. Hash: 3504224d
[01-06-2026 10-14] MODIFY — static/app.js — v46 — Panels: fixed wireToggles direct call (was only firing via PL_openNewPromptModal patch); added openPreviewModal/closePreviewModal shared modal; components panel COMPONENTS 10→16, FRAMEWORKS 6→9; context block click now shows preview before insert; workspace BLOCKS 12→18, FRAMEWORKS 6→9; workspace tile click opens preview before adding to canvas
[01-06-2026 10-14] MODIFY — static/app.css — v8 — Added #compPreviewModal + .cpm-* styles for the component preview modal

[01-06-2026 15-07] MODIFIED — start.bat — v2 saved — Added pywebview import check + force-reinstall on failure — Fixes ModuleNotFoundError: No module named 'webview' caused by corrupted ~ywebview partial install in system Python
[01-06-2026 18-37] EDIT — static/app.js — v48 saved — Fixed folder picker dropdown not appearing in New Prompt modal: replaced fragile DOM-property guard (_fpWired) with module-scoped boolean (_eventsWired); added renderDropdown('') call on every modal open so state.folders is always fresh — bug: dropdown silent on open after first use or async folder load
[01-06-2026 18-56] ROLLBACK — static/app.js — restored v40 — removed initFolderPicker combobox; reverted to plain <select> folder dropdown — folder search feature removed: non-functional
[01-06-2026 18-56] ROLLBACK — static/index.html — restored v92 — removed folder-picker-wrap/folderPickerInput combobox markup — reverted to plain <select id=promptFolder>
[01-06-2026 18-56] EDIT — static/app.css — v12 saved — stripped .folder-picker-* and .fp-* CSS rules (1432 chars) — cleanup after folder picker rollback

[01-06-2026 00-00] ADD — onboarding wizard — v100 (index.html), v48 (app.js) — 5-step first-run modal (Welcome → Add prompt → Variables → Chain → Done). Triggers after loadAll() on empty library. localStorage flag promptlib.onboarding_complete. Keyboard nav (arrows, Escape). Never shows again after dismissed.

[01-06-2026 00-00] ADD — bulk operations mode — v100 (index.html), v48 (app.js) — Checkbox select on prompt rows. Sticky action bar: Move to folder / Add tag / Export (Pro) / Delete. Select all toggle. Escape to exit. Confirmation on delete. Export Pro-gated.

[01-06-2026 00-00] ADD — Prompt Analyser workspace (Free) — v100 (index.html), v48 (app.js) — Full-screen workspace. Rule-based analysis of 5 components: Role, Task, Context, Format, Constraints. Green/amber/red indicators. Save improved version (user chooses overwrite or new). Accessible via Workspaces nav group.

[01-06-2026 00-00] ADD — Prompt Scorer workspace (Pro) — v100 (index.html), v48 (app.js) — Full-screen Pro-gated workspace. Rubric scoring 1-5 across 5 dimensions. Visual scorecard /25. Top improvement tip highlighted. Premium modal shown to free users.

[02-06-2026 session] FIX — static/app.js — v52 saved — Deep function polish: (1) Added contextBank/scorecard/collection to init() nav handler; (2) Added 3 new workspaces to _escapeToLibrary; (3) Wrote full Context Bank workspace IIFE (localStorage CRUD, category filter, copy, open/close, Escape); (4) Wrote full Scorecard workspace IIFE (6-dimension live scorer, search-to-load, copy report, clear, Escape); (5) Wrote full Collection Builder workspace IIFE (localStorage CRUD, prompt search-to-add, .plp export, Escape); (6) Wrote Modal Side Panels IIFE (ctxPanelToggleBtn, compPanelToggleBtn, close buttons, search, category filter, save new block, insert-to-textarea); (7) Fixed roleTypeInput appended to prompt_starter on save; (8) Fixed labNotes included in saveLabWinner description. Hash: 443a7bc8

[02-06-2026 session-2] ROLLBACK + REBUILD — static/app.js — v53 saved — Removed Scorecard and Collection Builder entirely. Rebuilt Context Bank and Prompt Components workspaces with correct architecture: (1) All new code is inside the outer IIFE, not appended as separate DOMContentLoaded listeners; (2) open/close/init functions follow existing patterns; (3) initContextBankWorkspace() + initModalSidePanels() + initComponentsWorkspace() called from BOOTSTRAP; (4) Context Bank modal side panel (ctxPanelToggleBtn) wired — inserts blocks into prompt textarea; (5) Components modal panel (compPanelToggleBtn) wired; (6) All nav views now have JS routes — zero orphaned nav buttons; (7) Architectural rules comment block added at top of file; (8) _escapeToLibrary updated for contextBank + components. Hash: 6243d056

[02-06-2026 session-2b] FIX — static/app.js — v54 saved — Restored full Prompt Components workspace from v47: 18 building blocks, 9 frameworks, drag-and-drop canvas, editable block cards, reorder, save to library. Removed redundant BOOTSTRAP initComponentsWorkspace() call (IIFE self-executes). Nav route and _escapeToLibrary entry retained. Hash: 95bb588c

[02-06-2026 session-3] EXPAND — static/app.js + app.css — v55 saved — Prompt Components: (1) Added 10 new building blocks: Chain of Thought, Tree of Thought, Self-Consistency, Assumption Audit, Devil's Advocate, First Principles, Socratic Method, Prompt Improver, Prompt Generator, Prompt Critic, Use Case Expander, Variable Extractor, System Prompt Architect (total: 30 blocks); (2) Added 5 new frameworks: TRACE, RODES, COSTAR+, ToT, META (total: 14 frameworks); (3) Fixed modal Components panel — exposes _pcwBLOCKS/_pcwFRAMEWORKS globals, renders compBlockGrid + frameworkList on panel open with click-to-insert; (4) Added .comp-block-btn + .comp-fw-btn + .comp-fw-badge CSS to app.css. Hash: [see below]

[02-06-2026 session-4] BUILD — static/app.js + index.html + app.css — v56/v99 saved — (1) Feature: drag blocks/frameworks from Components modal panel directly into prompt textarea — draggable="true" + dragstart on comp-block-btn/comp-fw-btn, dragover/dragleave/drop on #promptContent with .drag-over-active highlight; (2) Feature: interactive node canvas in Components workspace — unlimited pan/zoom (scroll wheel, alt+drag), SVG bezier connection lines with arrowheads, port circles to draw connections, node drag-to-reposition, double-click edge to delete, auto-layout, fit-to-screen, zoom toolbar, topological sort on save (follows connection order), canvas drop zone from palette; (3) CSS: pcw-node, pcw-node-header/body/ports/del, pcw-port, pcw-svg-layer, pcw-node-layer, pcw-canvas-toolbar, drag-over-active. Hash: 4432e4ce

[02-06-2026 session-5] UI POLISH — static/app.js + index.html + app.css — v57/v100 saved — Reverted node canvas to original card-based canvas (v55-backup). Applied UI improvements: (1) Palette search bar — filters blocks and frameworks live, hides empty sections; (2) Collapsible palette sections with item count badges; (3) Category colour strips on tiles (violet=thinking, cyan=meta, accent=core); (4) Canvas cards: accent-tinted header, ring hover glow, better shadow; (5) Footer: title input flex-grows, block count styled as pill; (6) Drop zone: larger hint icon, better empty state; (7) Blocks/frameworks updated data-cat attributes for colour coding. Hash: ad798a01

[03-06-2026 17-32] EDIT — static/index.html — v105 saved — replaced folderPickerWrap combobox (search input + hidden input + dropdown div) with native <select id=promptFolder class=form-input> — removes broken unwired search field; existing updateFolderDropdown() JS now populates select correctly

[03-06-2026 21-12] EDIT — static/app.js — v61 saved — fixed duplicate FRAMEWORKS bracket (syntax error); corrected RODES to PDF definition (Example+Steps not Examples+Style); added GRWC, ROSES, CSI+FBI frameworks; 15 new blocks across 4 categories; drag reorder fixed (stopPropagation + setData); up/down reorder buttons added to canvas cards
[03-06-2026 21-12] EDIT — static/app.css — v11 saved — added .pcw-block-order-btns and .pcw-block-reorder-btn styles for up/down canvas controls
[03-06-2026 21-12] EDIT — app.py — v21 saved — added 6 new schema columns on prompts (status, parent_id, prompt_domain, prompt_use_case, prompt_output_format, prompt_tone); 3 new columns on prompt_versions (version_label, version_notes, is_baseline); taxonomy_domains + taxonomy_use_cases + prompt_relationships tables; seeded 7 domains with 42 use cases; updated _prompt_payload, serialize_prompt, create/update/duplicate handlers; added /fork, /status, /taxonomy, /relationships endpoints
[03-06-2026 22-41] FIX — app.js — v58 (saved pre-splice) — Fixed broken regex escaping in _updateVarLivePreview: \[\[ was single-escaped (no-op in RegExp constructor), now correctly double-escaped \[\[ to match literal brackets. Also restored truncated file by splicing v58 (has preview feature) head with v57 (complete) tail at SMART TAG boundary — JS syntax clean.

[03-06-2026 23-12] ADD — app.js — v60 — 15 new building blocks added to Prompt Components (Writing, Research, Product/UX, Coaching, Specialised categories)
[03-06-2026 23-12] SORT — app.js — v60 — FRAMEWORKS array sorted alphabetically by badge (5W2H → ToT, 23 frameworks)
[03-06-2026 23-12] LAYOUT — app.css + index.html — v2/v107 — Components workspace restructured: palette top (stacked sections, horizontal wrap), canvas bottom with overflow-y scroll
[03-06-2026 23-12] NAV — index.html — v107 — Workspace nav reordered: Components → Chain → Context Bank → Forge → Lab → Metaprompting
[03-06-2026 23-12] PREMIUM — index.html — v107 — Context Bank and Prompt Components added to Free locked list and Pro feature list in premium modal
[04-06-2026 22-51] ADD — onboarding overlay HTML (index.html) — v108 rollback saved — Added #onboardingOverlay with spotlight, tooltip card, progress dots, skip/back/next buttons before #toastContainer. Added #tourBtn to sidebar footer. — First-run interactive app tour
[04-06-2026 22-51] ADD — onboarding CSS (app.css) — v13 rollback saved — Added spotlight, card, dot, and action button styles under /* ONBOARDING TOUR OVERLAY */ section — First-run interactive app tour
[04-06-2026 22-51] ADD — onboarding JS (app.js) — v65 rollback saved — Added IIFE with 13-step STEPS array, _positionSpotlight(), _positionCard(), _renderStep(), startOnboarding(), endOnboarding(), initOnboarding(). window.PL_startOnboarding exposed. initOnboarding() wired into BOOTSTRAP. localStorage flag promptlib.tourDone. — First-run interactive app tour

[05-06-2026 10-34] RESTORE — static/index.html — v108→v109 (patched) — File was truncated mid-tag (0 script tags, no </body></html>). Restored from v108 rollback, then added missing #promptViewer HTML and onboarding overlay HTML from forensic copies. Context Bank replaced with card grid layout (ctx-body-v2) recovered from v108-TRUNCATED-forensic-05jun.

[05-06-2026 10-34] RESTORE — static/app.js — v65→v67 (patched) — File was truncated mid-function (ended on open paren, syntax error). Restored from v65 rollback, then appended two missing IIFEs: ONBOARDING SPOTLIGHT TOUR (PL_startOnboarding, PL_onboardNext, PL_skipOnboarding, initOnboarding, 13-step STEPS array with clip-path spotlight) and PROMPT VIEWER (PL_openViewer, PL_closeViewer, initPromptViewer). Hash: b60c6210

[05-06-2026 10-34] FIX — static/app.css — deduplicated onboarding CSS — Removed two stale onboarding blocks (lines 5086-5308) that used .ob-active class and box-shadow black overlay (root cause of black screen bug). Kept only the correct block using .active class and clip-path. Removed 223 lines.

[05-06-2026 10-34] ADD — static/app.js — _renderCtxCardGrid() + _ctxUpdateStats() — Added card grid render function (window._renderCtxCardGrid) matching the ctx-body-v2 HTML layout: category badge, title, preview, footer with copy/edit buttons. Updated openContextBankWorkspace, _ctxOpenBlock, _ctxShowEditorEmpty, _ctxSaveBlock, _ctxDeleteBlock, ctxSearch, category filter to call card grid render. Hash: b60c6210

[05-06-2026 10-34] ADD — static/app.css — ctx-editor-v2-title + ctx-editor-v2-actions — Added missing CSS classes for the new Context Bank editor panel header and action bar. Hash: 528e072a

[05-06-2026 10-41] FIX — static/app.js + index.html — v69/v112 saved — Components modal side panel now matches workspace tile design. Rewrote _compPanelRender() to emit pcw-block-tile (icon + label, 2-col grid) and pcw-fw-tile (badge + name + desc, full-width) instead of comp-block-btn/comp-fw-btn. Added pcw-block-grid class to #compBlockGrid container. Hash: 6eab8841

[05-06-2026 12-55] ADD — static/app.js + index.html + app.css — v70/v114/v17 saved — Three new workspaces: (1) Mutation Engine (data-view="mutation") — 5 radical prompt rewrites: Invert the assumption, Extreme persona, Hard constraint, Time-shifted, Format break. Load from library or paste. Copy mutated version. Free tier. (2) Dead Prompts (data-view="deadPrompts") — scans library for prompts unused 30+ days, decay states: Sleeping/Fading/Dead, decay bar, detail panel with copy + open-in-editor. Free tier. (3) Blind Spot (data-view="blindSpot") — 5 analysis lenses: Assumed persona, Output format, Audience, Tone, Scope assumptions. Instant analysis on paste or load. Free tier. Hash: 1f85f6a0 / CSS: 624de59e

[05-06-2026 13-03] REMOVE — static/app.js + index.html + app.css — v71/v115/v18 saved — Removed Mutation Engine and Blind Spot workspaces entirely (JS IIFEs, HTML blocks, nav buttons, CSS sections). Fixed Dead Prompts workspace invisible body: .dp-body, .dp-list-panel, .dp-detail-panel now all have height:100%. Hash: 8446e224

[05-06-2026 13-03] POLISH — static/app.css — v19 saved — UI polish layer: btn border-radius bumped to --r-md, btn-accent/btn-primary depth shadows, nav-item active left-border signal (2px accent), search focus ring, form input focus ring, chip active state (filled accent, white text), prompt card grid active glow ring, ws-header box-shadow separator, ws-title display font, toasts: coloured left border per type + larger radius, modal backdrop blur, custom scrollbars, nav-sub-item tighter padding, premium lock icon opacity, folder-item active left-border, Dead Prompts entrance animation, onboarding card shadow/glow. CSS hash: 195af13a
[05-06-2026 BUILD] ADD — app.py — v22 saved — Added prompt_chains table + 4 Flask API routes (/api/chains GET/POST, /api/chains/<id> GET/DELETE) — Chain workspace persistence
[05-06-2026 BUILD] ADD — static/app.css — v14 saved — Added CSS for .chain-sidebar, .chain-assembly-panel, .chain-drag-handle, .chain-step-count, .chain-arrow-label, .chain-sidebar-locked, .chain-step-badge — Chain workspace enhancements
[05-06-2026 BUILD] MODIFY — static/index.html — v116 saved — Updated #chainWorkspace to two-column layout (sidebar + canvas), added assembly panel HTML, added Assemble Chain button to header — Chain workspace enhancements
[05-06-2026 BUILD] MODIFY — static/app.js — v77 saved — Added _chainWordCount(), updated _renderChainSteps() (drag handles, word counts, Output→Input labels, DnD reorder), added loadSavedChains(), _loadSavedChain(), _buildChainText(), assembleChain(), rewrote initChainWorkspace() with sidebar+assembly wiring — Chain workspace enhancements
[05-06-2026 session] MODIFY — static/index.html — v82 saved — Restored #chainWorkspace to clean single-column layout (removed .chain-sidebar/.chain-canvas split); added #chainNodesNavBtn to sidebar nav; added #chainNodesWorkspace HTML with node panel + flow canvas + assembly panel — Task 1 & 2 build
[05-06-2026 session] MODIFY — static/app.js — v69 saved — Added openChainNodesWorkspace(), closeChainNodesWorkspace(), _cnNodes state, _cnRender(), _cnRenderNodePanel(), _cnRenderFlowCanvas(), _cnBuildText(), initChainNodesWorkspace(); wired nav route v=chainNodes; added #chainNodesWorkspace to _escapeToLibrary(); added initChainNodesWorkspace() to BOOTSTRAP — Task 2 JS
[05-06-2026 session] MODIFY — static/app.css — v15 saved — Removed .chain-sidebar, .chain-sidebar-*, .chain-step-badge, .chain-sidebar-locked, .chain-canvas, .chain-drag-handle CSS blocks; removed .chain-body flex-direction override; appended full .cn-* CSS for Chain Nodes workspace — Task 1 & 2 CSS
[06-06-2026 00-00] MODIFY — static/app.css — v17 saved before edit — Replaced old chain workspace CSS block (chain-body, chain-seed, chain-step, chain-arrow, chain-handoff-btn, chain-final-output) with new pipeline redesign CSS (chain-step-label-input, chain-word-count, chain-connector, chain-add-inline, chain-preview-panel, chain-preview-ta, chain-steps-wrap) — chain workspace redesign
[06-06-2026 00-00] MODIFY — static/index.html — v118 saved before edit — Replaced #chainWorkspace div: added chainAssembleBtn, new chain-seed-label structure, chainAddInlineBtn, chainPreviewPanel with collapsed class, chainPreviewCloseBtn/CopyBtn/SaveBtn — chain workspace redesign
[06-06-2026 00-00] MODIFY — static/app.js — v82 saved before edit — Added _chainWordCount(), replaced _renderChainSteps() with accordion-style expand/collapse version, added assembleChain(), updated initChainWorkspace() with new event listeners, updated _chainAddStep() to set _expanded:true — chain workspace redesign
[07-06-2026 22-26] CREATE — neumorphism-preview.html — no version (new file) — Strong neumorphism preview of full Prompt Library UI: Space Mono font, #E7E5E4 surface, #006666 accent, 10px/20px dual shadow system, all Unicode icons — preview only, no live files touched
[07-06-2026 22-35] BACKUP — index.html v132 / app.css v18 / app.js v84 — pre-neumorphism blend — MD5-verified snapshots of all three live files before any design changes — rollback point for neumorphism selective integration work
[07-06-2026 22-42] MODIFY — app.css — v19 saved — neumorphism blend applied: strengthened neu tokens (5px→8px raised, 2px→5px inset), nav active inset, grid card active inset, chip base raised + active inset with accent — no HTML/JS touched
[07-06-2026 22-53] MODIFY — app.css — v20 saved — neumorphism nudge: tokens 8→10px raised/5→6px inset, sort selects recessed, view toggle container inset + active button raised
[07-06-2026 23-05] ROLLBACK — app.css — restored to v18 (pre-neumorphism blend) — user request — preparing for deployment debug
[08-06-2026 12-02] FIX — index.html — v133/v134 — removed stray </div> at contextBankWorkspace/componentsWorkspace boundary (line 1939) — div balance restored to 463/463 — pre-deployment debug
[08-06-2026 12-03] FIX — Main.py — v1/v2 — webview.start(true) → webview.start() on error path (line 109) — NameError in Python error recovery — pre-deployment debug
[09-06-2026 19-39] MODIFIED — PromptLibrary.iss — v4 saved — copyright lines (66, 133) updated to 'Eugene Phillips, trading as MX Phillips'; AppSupportURL and welcome-page feedback email changed from clientverse@proton.me to business email eugphill@gmail.com — proper attribution and business contact for the installer
[09-06-2026 19-57] EDIT — PromptLibrary.iss — v5 saved — added + concatenation operators to AppDescription multi-line define (lines 44-46) — ISPP does not auto-concatenate adjacent string literals; caused compiler error at line 44 col 158

[11-06-2026 15-20] FIX — static/app.js — v90 rollback saved — Stripped 2158 NUL bytes and removed resulting trailing empty line after })(); (was line 7571, broken node --check). File now 7570 lines, passes node --check. Root cause: NUL contamination from prior session's edit (Claude Code).
[11-06-2026 15-20] RESTORE — static/index.html — v100 rollback saved (broken live state, 1507 lines, missing componentsWorkspace + ~480 other lines) — Restored from v99 (1950 lines, 02-06-2026, last known-good with componentsWorkspace intact). Live file had been silently overwritten with an older/smaller version at some point after v99 with no changelog entry — root cause of 'broken' report. Post-restore: scripts=2, complete=True, NUL=0.
[11-06-2026 15-23] DELETE (versioned) — expand_components.py, expand_meta.py — v1 saved each (_rollbacks/expand_components.py/, _rollbacks/expand_meta.py/) — versioned then deletion attempted; root rm failed with "Operation not permitted" (Windows-mount permission limitation, same class as prior truncation issues — Linux shows -rwx------ but unlink is blocked) — files versioned and FLAGGED for manual deletion by Eugene via File Explorer; approved twice (prior session Q2, this session "yes to everything") — dead/unused, safe to delete once Eugene removes them.
[11-06-2026 15-23] CORRECTION — MEMORY.md — v1 rollback saved (_rollbacks/MEMORY.md/) — Prompt Components section (line 57) and Pro-features table (line 105) corrected from stale "15 categories/189 blocks/43 frameworks" (dated 2026-06-09, never matched live or backup state) to true live state: flat BLOCKS array (no cat tags, no CATEGORIES array), 23 FRAMEWORKS (5W2H→ToT, listed), renderPalette() targets #pcwBlockGrid/#pcwFwList, exposes window._pcwBLOCKS/_pcwFRAMEWORKS only (no _pcwCATEGORIES). 22-cat/295-block/51-framework expansion noted as built-but-unmerged (in _20260611_110936 backups), reframed as future planned work — why: MEMORY.md was independently inaccurate, unrelated to today's file corruption, and would have misled future sessions about app state.

[12-06-2026 14-51] FIX — static/app.js — v92 rollback saved — Root cause of blank-screen/0-prompts bug: extra `});` at line 5808 prematurely closed the outer IIFE (opened line 28), stranding ~1,760 lines (8 initXxxWorkspace functions incl. initCategoryChips, called first in BOOTSTRAP) in global scope, severed from $, $$, state, api, toast, escapeHtml. initCategoryChips threw uncaught ReferenceError on startup, aborting BOOTSTRAP before loadAll() ran — state.prompts stayed [], library rendered empty. Fix: moved the extra `});` from line 5808 to immediately before the final `})();` (line 7570) — net effect, all 8 stranded functions now inside IIFE (depth>=1 confirmed via custom scope-tracker), EOF balance (0,0), node --check passes, 0 NUL bytes. Diff vs v92: -1 line at 5808, +1 line at EOF, nothing else changed. update_hash.py run — new JS hash b5968966. Root cause found via mechanical depth-tracking + scope-reference analysis (engineering:debug skill, structured session), not visual inspection — invalidates and replaces the prior segment's incorrect "orphaned brace at 5807" diagnosis.
[13-06-2026 09-45] MERGE — static/index.html — v136-v139 rollbacks saved (one per step, pre-edit) — Restored ~9 days of unrolled feature work lost when index.html was frozen at v99 (02-06-2026, 1950 lines) while app.js progressed to 11-06-2026 referencing 22 IDs with no HTML counterpart. Source: _rollbacks/index.html/index.html - v134 (08-06-2026, 2100 lines). 4 merges: (1) REPLACE componentsWorkspace .ws-body.pcw-body (~50 lines) — dead node-canvas markup (pcwSvgLayer/pcwNodeLayer/pcwCanvasToolbar/zoom buttons, 0 app.js refs) swapped for v134's palette-search + drop-zone design (pcwPaletteSearch, pcwDropZone, pcwDropHint, pcwBlockSection/pcwFwSection via existing [data-toggle-section] handler) — v136; (2) ADD variable-fill preview to usePromptSection (varFillProgress/varFillProgressFill progress bar, varPreviewWrap/varPreviewBox live preview, varCopyFilledBtn) — v137; (3) ADD Context Bank stats row (ctxStatBlocks/ctxStatCats) below category pills and ctxEditorHeading eyebrow atop ctxEditorForm — v138; (4) ADD Chain assembly preview (chainAssembleBtn header button, chainAddInlineBtn, chainPreviewPanel with chainPreviewCloseBtn/chainPreviewText/chainPreviewCopyBtn/chainPreviewSaveBtn) to chainWorkspace — v139. All 22 target IDs verified present post-edit. app.css already had matching classes for all new markup — no CSS pulled, no duplicate rule blocks found (Key Principle #13 check clean). update_hash.py run — new hash b5968966. Post-edit: scripts=2, NUL=0, </body></html> present, div imbalance unchanged at -1 (pre-existing, confirmed matches v135 baseline, not introduced by this merge), node --check clean, app.py compiles clean. Out of scope (intentionally skipped, dead code, same treatment as Sprites): promptViewer modal (10 pv* IDs, never called from BOOTSTRAP) and 3 unrecoverable chain* IDs (chainFinalBody, chainFinalOutput, chainPromptSelect, not in v134 either). NOTE: first attempt at edit (2) via Edit tool silently truncated index.html to 1937 lines (scripts=0, no </body></html>) — caught immediately by verification, restored from v137, redone via Python content.replace() per Key Principle #10.
